<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Order Confirmation</title>

  <!-- Bootstrap -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >

  <style>
    body {
      background-color: #f8f9fa;
      font-family: "Poppins", sans-serif;
    }

    .order-container {
      max-width: 700px;
      margin: 50px auto;
      background: white;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      padding: 30px;
    }

    .order-header {
      text-align: center;
      color: #28a745;
    }

    .order-header i {
      font-size: 50px;
      color: #28a745;
      margin-bottom: 10px;
    }

    .address-box {
      background-color: #f1f3f5;
      padding: 15px;
      border-radius: 10px;
    }

    .product-item {
      border-bottom: 1px solid #ddd;
      padding: 10px 0;
    }

    .btn-continue {
      background-color: #F28123;
      color: white;
      font-weight: 500;
      border-radius: 30px;
    }

    .btn-continue:hover {
      background-color: #e06d10;
      color: white;
    }
      @media print {
    .btn, a.btn {
      display: none !important;
    }
  }
  </style>
</head>
<body>

  <div class="order-container">
    <div class="order-header">
      <i class="fas fa-check-circle"></i>
      <h3>Order Confirmed!</h3>
      <p>Thank you for your purchase, <strong><%= order.address.fullName %></strong>!</p>
    </div>

    <div class="order-details mt-4">
      <h5>Order Summary</h5>

      <p><strong>Order ID: #</strong> <%= order._id %></p>
      <p><strong>Payment Method: </strong> <%= order.paymentMethod %></p>
      <p><strong>Order Date: </strong><%= order.orderDate %></p>

      <hr />

      <h6>Shipping Address</h6>
      <div class="address-box mb-3">
        <p><strong><%= order.address.label %></strong></p>
        <p><%= order.address.street %>, <%= order.address.city %></p>
        <p><%= order.address.district %>, <%= order.address.district %> - <%= order.address.pincode %></p>
        <p>Phone: <%= order.address.phone %></p>
      </div>

      <h6>Ordered Items</h6>

      <% order.items.forEach(item => { %>

      <div class="product-item d-flex justify-content-between align-items-center">
        <div>
          <p class="mb-1 fw-semibold"><%= item.name %> (1kg)</p>
          <small>Qty: <%= item.quantity %></small>
        </div>
        <p class="fw-bold">₹<%= item.total %></p>
      </div>
        <% }) %>
      <hr />
      <h5 class="text-end">Total: ₹<%= order.totalAmount %></h5>

      <div class="text-center mt-4">
        <a href="/shop" class="btn btn-continue px-4 py-2">Continue Shopping</a>
      </div>
      <div class="text-center mt-3">
  <button class="btn btn-secondary px-4 py-2" onclick="window.print()">
    🖨️ Print this Page
  </button>
</div>
    </div>
  </div>


  <script src="https://kit.fontawesome.com/a2e0a4c123.js" crossorigin="anonymous"></script>
</body>
</html>
